services:
  api:
    build:
      context: .
      dockerfile: docker/Dockerfile.api
    environment:
      PHILOSOPHY_EXTRACTOR_API_ADDR: 0.0.0.0:8080
    ports:
      - "8080:8080"

  worker:
    build:
      context: .
      dockerfile: docker/Dockerfile.worker
    profiles:
      - worker
